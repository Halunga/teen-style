{
  "name": "stilkoll",
  "private": true,
  "scripts": {
    "refresh:catalog": "node scripts/refresh-catalog.mjs",
    "check": "node --check app.js && node --check scripts/refresh-catalog.mjs"
  },
  "devDependencies": {
    "playwright": "^1.58.2"
  }
}
